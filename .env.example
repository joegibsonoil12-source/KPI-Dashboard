# Supabase Configuration
VITE_SUPABASE_URL=your-supabase-project-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Billboard Configuration
# Auto-refresh interval in seconds (default: 15)
VITE_BILLBOARD_REFRESH_SEC=15

# Billboard API Base URL
# Optional: Specify the base URL for the Billboard API
# - Leave empty for default (relative path - works for Vercel/Netlify and local dev)
# - Set to your serverless function URL for GitHub Pages + external API deployments
# - Example: https://your-api.vercel.app or https://your-api.netlify.app
VITE_BILLBOARD_API_BASE=

# Billboard TV Mode Token (Frontend)
# Optional: Secret token for securing TV mode access
# This should match BILLBOARD_TV_TOKEN set in your serverless function environment
# The frontend uses this to append ?token=... when opening TV mode
# Recommendation: Use a long random string (e.g., 32+ characters)
# Example: openssl rand -base64 32
VITE_BILLBOARD_TV_TOKEN=

# Billboard TV Mode Token (Backend - Serverless Functions Only)
# Set this in your Vercel/Netlify deployment environment (NOT in .env for frontend)
# This is checked server-side when ?tv=1 is present in the request
# Must match VITE_BILLBOARD_TV_TOKEN for TV mode to work
# BILLBOARD_TV_TOKEN=your-secret-token-here

# Supabase Service Role Key (Backend - Serverless Functions Only)
# Set these in your Vercel/Netlify deployment environment (NOT in .env for frontend)
# Used by api/billboard-summary.js and netlify/functions/billboard-summary.js
# to query Supabase with elevated permissions
# SUPABASE_URL=your-supabase-project-url
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# QuickBooks Configuration (if using QuickBooks integration)
# VITE_QUICKBOOKS_CLIENT_ID=your-quickbooks-client-id
# VITE_QUICKBOOKS_CLIENT_SECRET=your-quickbooks-client-secret
# VITE_QUICKBOOKS_REDIRECT_URI=your-redirect-uri
